{
  "name": "@workspace/api-spec",
  "version": "0.0.0",
  "private": true,
  "scripts": {
    "codegen": "orval --config ./orval.config.ts && sed -i \"s|from 'zod'|from 'zod/v4'|\" ../api-zod/src/generated/api.ts && node -e \"const fs = require('node:fs'); const file = '../api-client-react/src/generated/api.ts'; fs.writeFileSync(file, fs.readFileSync(file, 'utf8').trimEnd() + '\\n');\" && pnpm -w run typecheck:libs"
  },
  "devDependencies": {
    "orval": "^8.23.0"
  }
}
